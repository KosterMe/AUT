"""What AUT still asks of a media file: where its pieces go, how long it runs.

Rendering moved to `montage.render`. `scenes.analyse` stayed, because a scene
change is a signal for *cutting* — deciding where a clip begins — and cutting
is AUT's (§3.4).
"""
